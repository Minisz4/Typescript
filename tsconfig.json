{
  "compilerOptions": {
    "target": "ES6", // Vilken JS version ska vi kompilera till
    "outDir": "./dist" // Var ska JS-filen hamna? I vilken mapp?
  },
  "include": ["./src", "index.ts"], // Vad för filer ska vi kompilera till JS?
  "exclude": [] // Vad ska vi inte kompilera?
}
